from setuptools import setup, find_packages

setup(
    name="MakeDjango",
    version="0.0.1",
    install_requires=[
        "asgiref==3.4.1",
        "pytz==2021.3",
    ],
    packages=find_packages()

)
